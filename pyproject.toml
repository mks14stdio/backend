[tool.poetry]
name = "project-backend"
version = "0.1.0"
description = "The good backend"
authors = ["Denis Sova <dsova@sfedu.ru>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"
fastapi = "^0.121.0"
uvicorn = "^0.38.0"
sqlalchemy = "^2.0.44"
asyncpg = "^0.30.0"
pydantic-settings = "^2.11.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
